apiVersion: 1
prune: true

datasources:
  - name: Prometheus
    uid: prometheus
    version: 1
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
